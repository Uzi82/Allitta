export type navEl = {
    title: string,
    img: string,
    active: string,
}

export type Props = {
    active: string
}

export type UserProps = {
    img?: FileList,
    name: string,
    status: string
}
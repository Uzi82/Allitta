export type Props = {
    children?: string | JSX.Element | JSX.Element[],
}
export type TextStyles = {
    $mb?: string
    $gray?: boolean
}
export type ICategory = {
    id: string,
    title: string
}